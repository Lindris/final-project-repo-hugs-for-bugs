import React from 'react'

// TODO: Subheader, paragraph, input fields for first name, last name and email, button to submit 
// Alignment: Names in HStack, all content in a VStack so they are stacked vertically 

const contactForm = () => {
    return (
        <div>
            
        </div>
    )
}

export default contactForm